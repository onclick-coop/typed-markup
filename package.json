{
  "name": "typed-markup",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "prepare": "tsc -p jsconfig.json --declaration --emitDeclarationOnly --outDir dist",
    "test": "vitest"
  },
  "files": [
    "index.js",
    "dist/"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./index.js"
    }
  },
  "devDependencies": {
    "@vitest/browser": "^4.1.4",
    "@vitest/browser-playwright": "^4.1.4",
    "playwright": "^1.59.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.4"
  }
}
